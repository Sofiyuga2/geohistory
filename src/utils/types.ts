export interface Pioneer {
    id: number,
    status: number,
    image: string,
    name: string,
    description: string,
    date_birthday: number,
    date_death: number
}

export interface Option {
    id: number,
    name: string
}